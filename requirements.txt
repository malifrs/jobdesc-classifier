# Streamlit (the app currently runs on 1.62.0).
streamlit>=1.57

# ML stack — versions pinned exactly because the joblib bundle
# (job_role_onet_complete.joblib) was saved with scikit-learn 1.9.0.
# Loading it with a different scikit-learn version is not guaranteed.
scikit-learn==1.9.0
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
joblib==1.5.3
